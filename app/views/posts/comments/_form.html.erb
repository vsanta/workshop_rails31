<%= simple_form_for [@post, Comment.new] do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :body %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>